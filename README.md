# Snake Game

A simple terminal-based snake game implemented in Python using the
curses module.

## Usage

Run the game:

```bash
python snake.py
```

Use the arrow keys to move the snake and press `q` to quit.
